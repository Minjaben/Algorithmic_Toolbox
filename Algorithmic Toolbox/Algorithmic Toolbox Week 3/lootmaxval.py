#Uses python3
import sys

data = list(map(int, sys.stdin.read().split()))
pointer = 2
capacity = data[1]
num_entries = data[0]
ratios = []*num_entries/2
for i in range(num_entries):
    if W == 0:
        return 0 #???
    
    while i <= num_entries:
        
        
        
print (data)
